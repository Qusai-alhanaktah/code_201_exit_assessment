'use strict';
var numbers = [];
var greates;
function Result(
    namber
    ){
this.namber = number;
numbers.push(this);
}
function greaterThan(){
    for (let i = 0; i < numbers.length; i++) {
       if (numbers[i] > number[i-1]) {
            greates = numbers[i];
       }
        
    }
}